# PROJECT_CONTEXT.md

## プロジェクト概要

このリポジトリは、奉仕時間・奉仕報告・月次目標・年次目標を管理するためのWebアプリです。

- リポジトリ: `hipchin/volunteer-tracker`
- デフォルトブランチ: `main`
- 配信想定: GitHub Pages
- 主な利用環境: iPhone Safari、ホーム画面追加後のPWA表示
- 主な目的: 奉仕時間の記録、月別集計、年度集計、奉仕報告の確認
- データ保存: ユーザー端末内のブラウザ `localStorage`
- サーバー保存: なし
- ユーザー認証: なし
- 外部API連携: なし

このアプリでは、既存ユーザーの端末内データを失わせないことを最優先にする。

---

## 現在の主要ファイル構成

```text
hipchin/volunteer-tracker
├─ index.html      (CSSはインライン化済み)
├─ manifest.json
├─ sw.js
└─ js/
   └─ app.js       (旧 storage.js / time.js / ui.js / app.js / app-version.js / carryover-update.js を統合)
```

2026-07-26のbundle-1で、起動時のリクエスト数削減のため `css/style.css` を `index.html` の `<head>` 内へインライン化し、`js/storage.js` `js/time.js` `js/ui.js` `js/app.js` `js/app-version.js` `js/carryover-update.js` の6ファイルを単一の `js/app.js` へ統合した。旧ファイルは削除済み。統合後もファイル内部はセクション区切りコメント（`// ==== storage.js ====` など）で旧ファイル単位の役割分担を残しており、実行順序（旧読み込み順）もそのまま維持している。

### 各ファイルの役割

#### `index.html`

画面構造を定義する中心ファイル。

主な画面は以下の4タブ。

- 記録
- 履歴
- 集計
- 設定

CSSは `<head>` 内にインライン化済み。JavaScriptは `js/app.js` を1本だけ読み込む。

#### `js/app.js`

旧6ファイルを統合した唯一のJavaScriptファイル。内部はセクションごとに分かれている。

- storage.js セクション: データ保存・読み込み・バックアップ作成を担当
- time.js セクション: 日付・時刻・月・年度・表示形式の補助処理を担当（年度は9月から翌8月まで）
- ui.js セクション: 画面描画・タブ切り替え・履歴表示・集計表示・トースト表示・アプリ情報表示を担当
- app.js セクション: `state` の初期化、タイマー操作、手動入力、記録の確定・編集・削除、バックアップ書き出し・読み込み、月移動、目標保存、Service Worker登録・更新バナー表示などアプリ全体の状態管理を担当
- app-version.js セクション: `VT_APP_BUILD` / `VT_APP_VERSION_LABEL` の一元管理
- carryover-update.js セクション: 野外奉仕の分数繰り越しパッチ（即時実行関数でグローバルを汚染しないよう分離されている）

セクション間の実行順序（旧ファイル読み込み順）に依存関係があるため、統合ファイル内での並び順を入れ替えない。

#### `manifest.json`

PWAとしてホーム画面追加するための設定。

主な設定。

- アプリ名: 奉仕時間記録
- 短縮名: 奉仕記録
- `start_url`: `./`
- `display`: `standalone`
- 背景色: `#f2f2f7`
- テーマ色: `#f2f2f7`
- 言語: 日本語
- アイコン: `/volunteer-tracker/apple-touch-icon-v2.png`

#### `sw.js`

Service Worker。

アプリシェルを端末内へ事前保存し、2回目以降の起動ではHTML・CSS・JavaScript・アイコンをキャッシュから読み込む。

画面遷移のリクエストではキャッシュ済みHTMLを即座に返し、バックグラウンドでGitHub Pages上のHTMLを取得して次回起動用に更新する。

旧キャッシュは、新しいService Workerが有効化された時点で削除する。

キャッシュ削除は `volunteer-tracker-` で始まるキャッシュ名だけを対象とし、同じGitHub Pagesドメイン上の別PWAへ影響させない。

---

## 主な機能

### 1. 奉仕時間の記録

ユーザーは奉仕時間を以下の方法で記録できる。

- タイマーで開始・終了を記録
- 日付・開始時刻・終了時刻を手動入力
- 記録確定前に中断時間を差し引く
- 計測中に開始時刻を修正
- 終了後に終了時刻を修正

カテゴリは以下の2種類。

- 野外奉仕
- その他の奉仕

### 2. 履歴管理

月ごとに奉仕記録を表示する。

履歴では以下が可能。

- 記録内容の確認
- 日付の修正
- 開始時刻の修正
- 終了時刻の修正
- 中断時間の修正
- カテゴリ変更
- 記録削除

表示上は、手動入力や編集済みの記録にバッジが付く。

### 3. 月別集計

選択月について以下を表示する。

- 合計時間
- 野外奉仕時間
- その他の奉仕時間
- レッスン件数
- 月次目標までの残り時間
- 奉仕報告テキスト
- 報告済み状態

### 4. 年度集計

年度は9月から翌年8月まで。

選択月が属する年度について以下を表示する。

- 年度ラベル
- 年度目標時間
- 年度合計時間
- 年度目標までの残り時間
- 年度進捗バー

現在の年次目標のデフォルトは600時間。

### 5. 奉仕報告の確認

前月分が未報告の場合、通知オーバーレイを表示する。

ユーザーは集計画面でレッスン件数を確認し、対象月を報告済みにできる。

報告通知は「あとで」を選ぶと当日分だけスヌーズされる。

### 6. 月次目標

設定画面で月次目標時間を変更できる。

現在のデフォルトは30時間。

月次目標はトップ画面と集計画面の残り時間計算に使用される。

### 7. 年次目標

設定画面で年次目標時間を変更できる。

現在のデフォルトは600時間。

年次目標は集計画面にのみ表示され、トップ画面には表示しない方針。

### 8. バックアップ

設定画面からJSONファイルを書き出せる。

バックアップに含める対象。

- アプリ識別子
- スキーマバージョン
- アプリバージョン
- 書き出し理由
- 書き出し日時
- 月次目標
- 年次目標
- レッスン件数
- 報告済み月
- 目標達成状態
- 奉仕記録一覧

### 9. バックアップ復元

JSONファイルから復元できる。

復元時は現在の記録が上書きされるため、復元前に端末内へ安全バックアップを保存する。

### 10. 最新版読み込み

設定画面から最新版を読み込める。

更新前にも端末内へ安全バックアップを保存する。

---

## データ保存方式

### 保存先

データはブラウザの `localStorage` に保存する。

サーバー、GitHub、外部DBには保存しない。

同じURLでも、端末・ブラウザ・PWA環境が変わると保存領域が別になる可能性がある。

そのため、機種変更・ブラウザ変更・リポジトリURL変更・PWA再追加時はバックアップと復元が重要。

---

## `localStorage` の主要キー

### `vt_sessions`

奉仕記録本体。

JSON配列として保存される。

各記録の主な項目。

- `id`
- `month`
- `date`
- `dateKey`
- `cat`
- `start`
- `end`
- `deductMin`
- `totalMin`
- `hours`
- `manual`
- `edited`

このキー名は変更しない。

### `vt_goal`

月次目標時間。

デフォルトは30。

### `vt_annual_goal`

年次目標時間。

デフォルトは600。

### `vt_lessons`

月ごとのレッスン件数。

月キーを使ったオブジェクト形式。

### `vt_reported_months`

月ごとの報告済み状態。

月キーを使ったオブジェクト形式。

### `vt_goal_status`

月ごとの目標達成状態。

目標達成バナーを重複表示しないために使う。

### `vt_active_timer`

計測中タイマーの復元用。

アプリを閉じた後でも、開始時刻とカテゴリを復元するために使う。

### `vt_pre_restore_backup`

バックアップ復元前の安全退避。

### `vt_pre_update_backup`

最新版読み込み前の安全退避。

### `vt_report_notice_snoozed_YYYY-MM`

奉仕報告通知の当日スヌーズ用。

### `vt_goal_achieved_YYYY-MM`

旧形式の目標達成状態。

現在は `vt_goal_status` へ移行しているが、互換処理が残っている。

削除しない。

---

## 記録データの形式

### 基本構造

奉仕記録は、概ね以下の情報を持つ。

- 一意のID
- 対象月
- 日付
- カテゴリ
- 開始時刻
- 終了時刻
- 中断時間
- 実質奉仕時間
- 手動入力かどうか
- 編集済みかどうか

### 互換補完

過去データに不足項目がある場合、読み込み時に補完する設計になっている。

補完される可能性がある項目。

- `id`
- `dateKey`
- `month`
- `date`
- `cat`
- `deductMin`
- `totalMin`
- `hours`
- `manual`
- `edited`

この補完処理は既存ユーザー保護のために重要。

新機能追加時も、過去データを読み込めるようにする。

---

## 年度仕様

年度は9月開始、翌年8月終了。

例。

- 2026年9月から2027年8月まで: 2026年度
- 2027年1月: 2026年度
- 2027年8月: 2026年度
- 2027年9月: 2027年度

年度集計は、選択中の月が属する年度を対象にする。

年次目標はトップ画面には表示せず、集計画面に表示する。

---

## 時刻・日付仕様

### 日付またぎ

日付をまたぐ記録は現在の仕様では対応しない。

タイマーで日付をまたいだ場合は、手動入力を促す。

手動入力では、終了時刻が開始時刻以下の場合は保存しない。

### 時刻形式

時刻は `HH:MM` 形式を前提にする。

### 日付キー

日付キーは `YYYY-MM-DD` 形式。

月キーは `YYYY-MM` 形式。

---

## PWA関連仕様

### `index.html` 側

iPhoneホーム画面追加を考慮した設定がある。

- `apple-mobile-web-app-capable`: `yes`
- `apple-mobile-web-app-status-bar-style`: `default`
- `apple-mobile-web-app-title`: `奉仕記録`
- `theme-color`: `#f2f2f7`
- Apple Touch Icon: `/volunteer-tracker/apple-touch-icon-v2.png`
- Manifest: `manifest.json`

### `manifest.json` 側

PWA表示の中心設定。

- `display`: `standalone`
- `start_url`: `./`
- `background_color`: `#f2f2f7`
- `theme_color`: `#f2f2f7`

### Service Worker

現在のService Workerは、起動高速化とオフライン起動のためにアプリシェルをキャッシュする。

キャッシュ対象は `index.html`、`manifest.json`、`js/app.js`、アプリアイコン。CSSは `index.html` にインライン化済みのため個別キャッシュ対象ではない。

通常起動ではキャッシュ済みHTMLを先に表示し、ネットワーク上のHTMLはバックグラウンドで取得して次回起動用に保存する。

`js/app.js` はバージョンクエリ付きURLをキャッシュする。更新時は `index.html` のクエリと `sw.js` の `CACHE_VERSION` を同時に更新する。

バックグラウンドで新しいService Workerが有効化され `navigator.serviceWorker` の `controller` が切り替わったとき（`controllerchange` イベント）、画面上部に「新しいバージョンがあります」バナーを表示する。初回インストール時（まだ制御されていない状態からの初回制御開始）には表示しないよう、ページ読み込み開始時点で既にService Workerに制御されていたか（`vtHadControllerAtLoad`）で判定する。バナーの「今すぐ更新」は `location.reload()` のみを行う（新しいアプリシェルは既に新しいService Workerのキャッシュへ保存済みのため、通常のリロードで反映される）。設定画面の「最新版を読み込む」とは別物で、そちらはキャッシュとService Worker登録自体を解除する、より強制力の強い更新導線として残す。

設定画面の「最新版を読み込む」では、安全バックアップ作成後にCache StorageとService Worker登録を解除し、キャッシュ回避URLで最新版を再取得する。

### 更新時の注意

JavaScriptのURLにはバージョン用クエリが付いている。

例。

- `js/app.js?v=20260726-2`

ファイル修正時は、このバージョン文字列も更新する。

更新しないと、iPhone Safariやホーム画面アプリで古いファイルが残る可能性がある。

Service Workerの `CACHE_VERSION` も、キャッシュ挙動を変える場合は更新対象。

---

## UI設計方針

### 最優先環境

- iPhone Safari
- iPhoneホーム画面追加後のPWA表示
- 片手操作
- 非エンジニア利用者

### 基本方針

- スマホで押しやすいボタンサイズを維持する
- 下部タブの安全領域を確保する
- 入力欄はiOSの挙動を考慮する
- 文字は小さくしすぎない
- 操作後はトーストで反応を返す
- 重要な破壊操作は確認ダイアログを出す

### 既存UIの特徴

- 最大幅430pxのスマホ中心レイアウト
- 下部固定タブ
- カード型表示
- iOS風の背景色と余白
- セーフエリア対応
- タップ時の押下フィードバック
- 目標達成時のオーバーレイ
- 未報告通知オーバーレイ

---

## 今後修正するときの最重要注意点

### 1. 既存データを消さない

最優先。

以下は無断で行わない。

- `localStorage.clear()`
- `vt_sessions` の削除
- 既存キー名の変更
- 既存データ形式の破壊的変更
- 復元処理の簡略化
- 互換補完処理の削除

### 2. データ構造を変える場合は移行処理を入れる

新しい項目を追加する場合は、過去データにその項目がなくても動くようにする。

既存の `loadSessions()` の補完方針に合わせる。

### 3. URL変更は慎重に扱う

GitHub PagesのURLやリポジトリ名が変わると、PWAやブラウザの保存領域が変わる可能性がある。

利用者にはバックアップを書き出してから移行してもらう必要がある。

### 4. PWA表示は通常ブラウザ表示と違う

iPhoneでは、Safariで開いた場合とホーム画面から開いた場合で見え方やキャッシュ挙動が異なる可能性がある。

修正確認は最低限以下で行う。

- PCブラウザ
- iPhone Safari
- iPhoneホーム画面追加後のPWA

### 5. Service Workerのバージョンを必ず同期する

アプリシェルキャッシュを使用しているため、ファイル変更時にバージョン更新を忘れると古いHTML・CSS・JavaScriptが残る可能性がある。

更新時は以下を同時に変更する。

- `index.html` のJavaScript用クエリ
- `js/app.js` 内（app-version.js セクション）の `VT_APP_BUILD`
- `js/app.js` 内（app-version.js セクション）の `VT_APP_VERSION_LABEL`
- `js/app.js` 内（storage.js セクション）の `APP_VERSION`
- `sw.js` の `CACHE_VERSION`
- `sw.js` の `APP_SHELL` 内のクエリ

更新導線とキャッシュ破棄処理は削除しない。

### 6. `README.md` を現在仕様の根拠にしない

現在の `README.md` は古い単一HTML版の内容が残っている可能性がある。

今後の仕様確認では、実ファイルを優先する。

優先順位。

1. 現在の実ファイル
2. この `PROJECT_CONTEXT.md`
3. 過去の会話メモ
4. `README.md`

### 7. 直接GitHubに変更しない運用を基本にする

新しいコードや修正ファイルは、原則としてユーザーが手動アップロードできる形で提供する。

GitHub上で直接変更する場合は、事前に以下を明示する。

- 変更目的
- 変更ファイル
- 影響範囲
- リスク
- 戻し方

### 8. 破壊的操作には必ず確認を入れる

記録削除、復元、データ上書き、保存領域変更に関わる操作では確認を入れる。

ユーザーが誤操作しても戻れる設計を優先する。

### 9. バックアップ機能を軽視しない

このアプリは端末内保存のため、バックアップが実質的な命綱。

以下の修正ではバックアップ対象に漏れがないか確認する。

- 新しい保存キーを追加したとき
- 新しい設定項目を追加したとき
- 新しい集計状態を追加したとき
- 報告・通知・目標達成状態を変更したとき

### 10. 表示だけの変更でもiPhone確認を重視する

PCでは問題なくても、iPhoneでは以下が崩れることがある。

- 下部タブ
- セーフエリア
- 入力欄
- 日付入力
- 時刻入力
- 固定ヘッダー
- キーボード表示時のスクロール
- ホーム画面PWA表示

---

## 今後の変更提案時に必ず整理する項目

今後、このリポジトリに変更を加える場合は、毎回以下を整理してから進める。

### 目的

何を改善するのか。

### 対象ファイル

どのファイルを変更するのか。

### 影響範囲

どの画面・機能・保存データに影響するのか。

### データ保護

既存の `localStorage` データに影響があるか。

### PWA影響

iPhone Safari、ホーム画面追加後の表示、キャッシュに影響があるか。

### リスク

壊れる可能性がある箇所。

### 戻し方

問題が起きた場合に元へ戻す方法。

### テスト項目

確認すべき操作。

---

## 変更提案の評価軸

各修正案は以下の3軸で評価する。

### 安全度

既存データ・既存機能・PWA表示を壊す可能性が低いか。

### 実装難度

修正範囲が小さいか。

依存関係が複雑でないか。

### ユーザー影響

利用者が操作で迷わないか。

既存ユーザーに再インストールや再設定を求める必要があるか。

---

## 現在の安全度評価

### 安全度が高い変更

- 文言修正
- 色や余白の軽微な調整
- ボタンラベル変更
- 集計画面の表示順変更
- 既存データを読むだけの表示追加
- 新しい説明文の追加

### 注意が必要な変更

- 記録データ形式の変更
- 保存キーの追加
- バックアップ形式の変更
- Service Worker変更
- PWA manifest変更
- URLやファイルパス変更
- 月次・年度集計ロジック変更
- 日付またぎ対応

### 安全度が低い変更

- `localStorage` の初期化
- `vt_sessions` の形式変更
- 既存キーの削除
- 復元処理の大幅変更
- Service Workerによる本格オフラインキャッシュ化
- リポジトリ名変更
- GitHub Pages配信パス変更

---

## 手動アップロード運用の注意

ユーザーがファイルを手動でアップロードする前提では、以下を守る。

- 変更ファイルを明確に分ける
- 変更対象外ファイルは触らない
- 全文差し替えの場合は対象ファイル名を明示する
- 複数ファイルを変更する場合はアップロード順を明示する
- CSS/JSのバージョンクエリ更新を忘れない
- 変更後にGitHub Pages反映まで少し待つ必要がある場合がある
- iPhone側で最新版読み込みボタンを使う
- それでも反映されない場合はSafariキャッシュやPWA再追加を疑う

---

## テスト観点

### 記録タブ

- タイマー開始できる
- タイマー終了できる
- 開始時刻を修正できる
- 終了時刻を修正できる
- 中断時間を追加できる
- 中断時間を差し引いて保存できる
- 手動入力で保存できる
- 終了時刻が開始時刻以前の場合は保存されない
- 日付またぎ時に手動入力へ誘導される

### 履歴タブ

- 当月の記録が表示される
- 月移動できる
- 記録を編集できる
- 編集後に合計が更新される
- 記録を削除できる
- 削除後に合計が更新される

### 集計タブ

- 月合計が正しい
- 野外奉仕が正しい
- その他の奉仕が正しい
- レッスン件数を保存できる
- 報告済みにできる
- 年度合計が正しい
- 年度目標までの残り時間が正しい
- 9月から翌8月の年度判定が正しい

### 設定タブ

- 月次目標を保存できる
- 年次目標を保存できる
- バックアップを書き出せる
- バックアップを読み込める
- 復元前バックアップが作られる
- 最新版読み込み前バックアップが作られる
- アプリバージョンが表示される

### PWA

- iPhone Safariで表示できる
- ホーム画面に追加できる
- ホーム画面から開くとSafariの上下UIが出ない
- 下部タブがセーフエリアに隠れない
- 更新後に最新版が反映される

---

## 開発時の基本姿勢

このアプリは複雑なサーバーアプリではなく、端末内保存の小さなPWAである。

そのため、最も重要なのは高度な機能追加ではなく、利用者の記録を守ること。

優先順位は以下。

1. 既存データを守る
2. iPhoneで使いやすくする
3. 操作を迷わせない
4. バックアップで逃げ道を残す
5. 機能追加は小さく安全に行う
6. キャッシュやPWA更新で利用者を詰ませない

---

## 禁止事項

以下は禁止。

- 既存データを消す可能性がある変更を無断で行う
- `localStorage.clear()` を使う
- 既存保存キーを削除する
- 既存保存キーの意味を変える
- 互換処理を削除する
- バックアップ対象を減らす
- APIキーや個人情報をフロントエンドに置く
- Service Workerを安易にキャッシュ優先へ変更する
- iPhone PWAで確認せずに表示変更を完了扱いにする
- 変更理由を説明せずにコードだけ出す
- 「たぶん動く」で済ませる
